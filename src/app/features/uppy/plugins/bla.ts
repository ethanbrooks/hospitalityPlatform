/*
import { Plugin } from '@uppy/core';

export class NumFiles extends Plugin {
  render() {
    const numFiles = Object.keys(this.uppy.state.files).length;
    return ('<div>Number of files: {numFiles}</div>');
  }
}
*/
